# Homework — 03.09.2026

## Topic
Study the fundamentals of **HTTP methods**, **REST API**, **RESTful architecture**, **OpenAPI**, **Swagger**, **JSON format for APIs**, and **CRUD operations**.

5. Swagger and OpenAPI (Swagger docs): https://swagger.io/docs/specification/about/
6. Swagger UI (official): https://swagger.io/tools/swagger-ui/
7. FastAPI OpenAPI docs: https://fastapi.tiangolo.com/tutorial/first-steps/#openapi
8. JSON intro (MDN): https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Scripting/JSON
9. JSON syntax and examples (json.org): https://www.json.org/json-en.html
10. CRUD explained: https://www.codecademy.com/article/what-is-crud
11. CRUD vs HTTP methods: https://restfulapi.net/http-methods/

## Homework tasks

1. Read all resources above and prepare short notes (5–10 bullet points) for each topic:
   - What is HTTP and why methods matter
   - Difference between REST API and RESTful architecture
   - What OpenAPI describes
   - What Swagger tools are used for
   - How JSON request/response bodies are structured in REST APIs
   - What CRUD means and how each CRUD operation maps to HTTP methods

2. In your FastAPI project, create a **Students API** with endpoints:
   - `GET /api/v1/students`
   - `GET /api/v1/students/{id}`
   - `POST /api/v1/students`
   - `PUT /api/v1/students/{id}`
   - `DELETE /api/v1/students/{id}`

3. Store data in memory (no real database).  
   Student fields:
   - `id` (integer)
   - `first_name` (string)
   - `last_name` (string)
   - `email` (string, valid email format)
   - `age` (integer)

4. Add validation and proper HTTP status codes:
   - `200` for successful GET/PUT
   - `201` for successful POST
   - `204` for successful DELETE
   - `404` when student is not found
   - `422` for invalid input

5. Open Swagger UI (`/docs`) and test every endpoint manually.

6. Export OpenAPI schema from your running app (`/openapi.json`) and save it as `openapi.json` in your project root.

7. Write a short report (`homework-report.md`) that includes:
   - examples of 2 requests and 2 responses (JSON)
   - one screenshot from Swagger UI
   - 3–5 sentences: what you learned and what was difficult

## Submission requirements

1. Push your code to your repository.
2. Include:
   - working Students CRUD API
   - `openapi.json`
   - `homework-report.md`
3. Be ready to explain your endpoint design in class.

## Evaluation criteria

1. Correct CRUD behavior and routing — 40%
2. Correct use of HTTP methods/status codes — 20%
3. Input validation and error handling — 20%
4. OpenAPI/Swagger usage and report quality — 20%